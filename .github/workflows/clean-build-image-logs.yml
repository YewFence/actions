name: Clean Build Image Logs

on:
  workflow_run:
    workflows:
      - Build Container Images
    types:
      - completed

permissions: {}

jobs:
  delete-logs:
    # Keep logs of manually dispatched runs for debugging.
    if: github.event.workflow_run.event != 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - name: Delete workflow run logs
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          RUN_ID: ${{ github.event.workflow_run.id }}
        run: gh api -X DELETE "/repos/$REPO/actions/runs/$RUN_ID/logs"
